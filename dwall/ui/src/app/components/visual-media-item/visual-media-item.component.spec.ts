/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VisualMediaItemComponent } from './visual-media-item.component';

describe('VisualMediaItemComponent', () => {
  let component: VisualMediaItemComponent;
  let fixture: ComponentFixture<VisualMediaItemComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ VisualMediaItemComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(VisualMediaItemComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
*/