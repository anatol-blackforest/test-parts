/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FormsModule } from '@angular/forms';

import { LineMediaItemComponent } from './line-media-item.component';

import { SizeCountPipe } from '../../pipes/size-count/size-count.pipe';
import { CountVideoDurationPipe } from '../../pipes/count-video-duration/count-video-duration.pipe';

describe('LineMediaItemComponent', () => {
  let component: LineMediaItemComponent;
  let fixture: ComponentFixture<LineMediaItemComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      imports: [ FormsModule ],
      declarations: [ 
        LineMediaItemComponent,
        SizeCountPipe,
        CountVideoDurationPipe
      ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(LineMediaItemComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    component.mode = 'show';
    component.media = {
      mediaType: 'image'
    }
    expect(true).toBeTruthy();
  });
});
*/