/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TvListItemComponent } from './tv-list-item.component';

describe('TvListItemComponent', () => {
  let component: TvListItemComponent;
  let fixture: ComponentFixture<TvListItemComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TvListItemComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TvListItemComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
*/