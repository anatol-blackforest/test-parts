/*import { StatusStyleDirective } from './status-style.directive';

describe('StatusStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusStyleDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
*/